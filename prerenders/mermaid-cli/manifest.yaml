name: mermaid-cli
provides: [diagram.mermaid]
matches:
  extensions: [.mmd, .mermaid]
backend:
  type: cli
  cmd: mmdc
  args:
    - "-i"
    - "{input-file}"
    - "-o"
    - "{output}"
    - "--quiet"
    - "{theme-config-args}"
theme_config: mermaid
requires:
  binaries: [mmdc]
priority: 50
